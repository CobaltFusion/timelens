[project]
name = "timelens"
version = "0.1.0"

dependencies = [
    "fastapi",
    "watchfiles",
    "uvicorn",
    "psutil",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "pytest-asyncio",
]
